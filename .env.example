# Proveedor a usar en main.py: "openai" o "anthropic"
LLM_PROVIDER=openai

# Modelos por defecto (podés cambiarlos)
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-sonnet-5

# API keys — completar con las propias, nunca commitear el .env real
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
